"use strict";
const suma = (x, y) => x + y;
const nombreCompleto = (nombre, apellidos) => `${nombre} ${apellidos}`;
alert(`Te llamas ${nombreCompleto("Laura", "Ortega")} y tu número favorito es ${suma(3, 5)}`);
